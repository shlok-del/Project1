PORT=5000
NODE_ENV=development

# Frontend URL for CORS (use * or your local dev url for testing)
CORS_ORIGIN=http://localhost:5173

# Local MySQL Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=portfolio_db

# Production Database URL (Provided automatically by Railway/Render)
# DATABASE_URL=mysql://user:pass@host:port/dbname
